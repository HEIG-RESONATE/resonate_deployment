# Copy to config/api.env and set real values.
# Loaded into the api container via env_file.

# Password for the admin UI login (POST /api/admin/login)
ADMIN_PASSWORD=change_me

# Secret used to sign admin JWTs
ADMIN_SECRET_KEY=change_me
