# Copy to config/secrets.env and fill in real values.
# Passed to the MCP gateway (--secrets) and exposed to MCP servers that
# declare the corresponding secret names in the catalog.
# One KEY=value per line, e.g.:
# brave.api_key=your_key_here
